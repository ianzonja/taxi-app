{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':(exclude)api'",
  "rewrites": [
    { "source": "/(.*)", "destination": "/$1.html" }
  ]
}
